///
/// Controller for menu
///
angular.module('monackfr')
    .controller('menuController',
    [
        "$scope", "$state", "usersApi",
        function ($scope, $state, usersApi) {
        }
    ]);